# General application configurations
- application:
    name: Flaskbox API

# The /users rout
- route:
    name: users
    fields:
    - name: string
    - last_name: string
    - users: array_str
    - ids: array_int
    - created_at: datetime
